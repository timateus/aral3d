

## Change 21c Lakes color from pink to blue

Two files need updating:

1. **`src/components/GeoFeatures.tsx`** — Change the fill color from `#ff69b4` to `#4fc3f7` (light blue) and the outline color from `#ff85c8` to `#81d4fa` (lighter blue).

2. **`src/components/Legend.tsx`** — Update the legend swatch from `#ff69b4` fill / `#ff85c880` border to match the new blue colors.

